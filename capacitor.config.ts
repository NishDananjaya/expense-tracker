import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luxe.expensetracker',
  appName: 'Luxe Expense Tracker',
  webDir: 'dist'
};

export default config;
